# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build dirS3Sync`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t dirS3Sync`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3002

WORKDIR /app

RUN addgroup --system dirS3Sync && \
          adduser --system -G dirS3Sync dirS3Sync

COPY dist/apps/dirS3Sync dirS3Sync/
RUN chown -R dirS3Sync:dirS3Sync .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix dirS3Sync --omit=dev -f install

CMD [ "node", "dirS3Sync" ]
